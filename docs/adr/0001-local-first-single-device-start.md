# ローカル完結・単一端末から始め、同期／アカウントは後回し

個人向けMVPとして、サーバー・アカウント・多端末同期を持たず、データは端末内に保存する形で始める。多端末同期と個人データのサーバー常駐こそがセキュリティと運用責任の主要因であり、コア（取り込み→抽出→カレンダー表示）を検証する前にそれを背負うのは過剰だから。

## Consequences

- ストレージはインターフェースの裏に置き、後から同期を差し替えで足せるようにする（本体を作り直さない）。
- スマホ対応・プラットフォーム化（ジャンル分けした個人データ基盤の構想）は、コア検証後の将来課題とする。
