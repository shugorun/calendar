# 自前カレンダーUIを持つ（Googleカレンダーに乗せない）

予定の表示は既存のGoogleカレンダーに書き込むのではなく、アプリ内に自前のカレンダーUIを描く。このアプリの中心価値である独自の状態軸——コミット軸（浮いている↔確定）と日時の確かさ軸（日時確定↔日時未定）——がGoogleカレンダーでは表現できないから。

## Consequences

- カレンダー描画を自前で実装する負担を負う。
- 確定済み予定をGoogleカレンダーへ書き出す一方向エクスポートは、将来の追加機能とする（普段使いはGCal、状態管理は自前、の良いとこ取り）。
