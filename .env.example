# 抽出に使う Vision LLM のプロバイダと鍵。実際の値は .env に書く（.env は git 管理外）。
# プロバイダは抽出アダプタ裏で差し替え可能（ADR-0002）。ステップ3で使う。

# 使うプロバイダ: anthropic | gemini
EXTRACTION_PROVIDER=anthropic

# どちらか、使う方の鍵を設定する
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
