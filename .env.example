# 抽出に使う Vision LLM の設定。実際の値は .env に書く（.env は git 管理外）。
# プロバイダは抽出アダプタ裏で差し替え可能（ADR-0002）。鍵が無ければダミー抽出に退避する。

# 使うプロバイダ（未設定なら gemini 扱い）
EXTRACTION_PROVIDER=gemini

# Gemini を使う場合は必須
GEMINI_API_KEY=

# 任意: 使うモデル（未設定なら gemini-2.5-flash）
GEMINI_MODEL=
